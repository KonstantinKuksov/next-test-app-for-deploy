import MainContainer from './../components/MainContainer';

const Index = () => {
  return (
    <MainContainer title={'Main page'} keywords={'Main page, Next App'}>
      <h1>Main page</h1>
    </MainContainer>
  );
};

export default Index;
